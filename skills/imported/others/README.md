# Other Curated Sources

Original upstream skill packages selected from excellent sources that do not yet justify a dedicated top-level source namespace.

Current examples include ByteDance DeerFlow, Figma ecosystem sources and Novoads.

See `registry/sources.json` and each package's `SOURCE.json` for provenance. Preserve upstream package structure and authorship.
