# Role Bundles

Role Bundles compose existing capabilities and Operator Packs into recognizable jobs. They do not duplicate skill bodies and they do not become giant always-on persona prompts.

The authoritative bundle definitions are `registry/roles.json`.
